// to track how many bfs waves were executed


int levels = 0;
while (!q.empty()) {
    int size = q.size(); // The "Snapshot"
    for (int i = 0; i < size; i++) {
        // 1. Pop current node
        // 2. Explore neighbors
        // 3. Push valid neighbors to queue
    }
    if (!q.empty()) levels++; // If new neighbors were added, time moves forward
}