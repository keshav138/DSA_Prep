Plan-------------
1. Second part of min cost coin, here we take the total ways they can be formed, only mind the count in teh recursino
2. We dont do a +1 every call , only on the last base case if the amount is 0.


----------CODE-------------------------
class Solution {
public:

    int solve(int idx, int amount, vector<int>&coins, vector<vector<int>>&dp){
        if(idx < 0){
            if(amount == 0) return 1;
            return 0; 
        }

        if(dp[idx][amount]!=-1) return dp[idx][amount];

        int notTake = 0 + solve(idx-1, amount, coins, dp);
        int take = 0;
        if(coins[idx] <= amount){
            take = solve(idx, amount-coins[idx], coins, dp);
        }

        return dp[idx][amount] = notTake + take;
    }

    int change(int amount, vector<int>& coins) {
        int n = coins.size();
        vector<vector<int>>dp(n, vector<int>(amount+1, -1));
        int ans = solve(n-1, amount, coins, dp);
        return ans;
    }
};