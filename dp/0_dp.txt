1. Memoization to Tabulation
-> Create the same dp array or table.
-> Update base case values from recursion in the dp array.
-> We incrementally build from base cases up to n
-> Loop from values after base case to n
-> Instead of using function calls replace them with the dp array calls.